<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Parking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Parking" parent="Base.Theme.Parking" />

    <style name="Theme.AppCompat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pomegranate</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_park_me_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Parking</item>
    </style>
</resources>